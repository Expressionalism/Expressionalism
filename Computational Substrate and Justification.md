# **Expressionalism Computational Substrate and Parameter Justification Appendix**

Expressionalism: Derivation of All Operational Parameters from the Philosophical Commitments, General Formal Apparatus, and Empirical Validation Results

(Provisional — Subject to Renewal under Presumption 8\)

## **Executive Summary**

This appendix supplies the deductive justification for every numerical and structural parameter instantiated in the Expressionalism Toolkit. All specifications are exhibited as lawful specializations of the general hybrid function H(x)  H(x)  H(x), the dilemma gate, the relational density spectrum of Presumption 3, the hard isolation rule, and the anchor priority of the current primary expression under Presumption 5\.

Every parameter satisfies seven jointly necessary and sufficient design criteria that preserve hard isolation semantics, the two-mode restriction with Isolation Residual (isolation density / density of Synthesis Refusal), the primary-as-anchor priority, equity toward non-relational remainders, bounded sensitivity, revisability under Presumption 8, and explicit derivability from the general apparatus. The appendix also closes the formal-operational disconnect by mapping the Toolkit as an operational specialization of that apparatus. The bootstrap/origination asymmetry and the methodological character of the expression primitive receive explicit acknowledgment. The justification is self-consistent, invokes no external authority, remains a representation rather than a higher foundation, and is fully provisional under Presumption 8\.

## **1\. Scope and Method of Justification**

The Expressionalism system must satisfy two interlocking requirements. First, all operational parameters must be shown to follow necessarily from the philosophical commitments and formal apparatus rather than from ad hoc selection. Second, every concrete element of the Toolkit must be exhibited as a direct specialization of the general hybrid function

H(x)=min⁡(1, μ(x)×Pnorm(x∣V))∈\[0,1\]H(x) \= \\min\\bigl(1,\\ \\mu(x) \\times P\_{\\rm norm}(x \\mid V)\\bigr) \\in \[0,1\]H(x)=min(1, μ(x)×Pnorm​(x∣V))∈\[0,1\]

the dilemma gate, the relational density spectrum, and the hard isolation rule. This appendix accomplishes both tasks through strict deductive order: foundational principles → design criteria → parameter-by-parameter derivation → exhaustive mapping → governance protocol → irreducible limits → conclusion. All claims advanced here are themselves expressions subject to the schema they describe and remain provisional under Presumption 8\.

## **2\. Foundational Principles and Constraints**

The Framework establishes expression — any provisional stabilization of secondary synthesis — as the fundamental primitive, with pointing as its directed, representational, or reflexive aspect. Pointing operates in one of two primary modes: self-pointing (reflexive identity) or other-pointing (distinct repeatable references). Residual openness remains the horizon or limit of obtainability. Isolation Residual (isolation density) sits outside the two-mode distribution and is not a third peer mode.

The formal apparatus supplies the hybrid function H(x)  H(x)  H(x), the dilemma gate controlled by threshold τ  \\tau  τ, and the hard isolation rule. When H(x)\>τ  H(x) \> \\tau  H(x)\>τ, the analysis proceeds with relational construction. When H(x)≤τ  H(x) \\leq \\tau  H(x)≤τ, the analysis registers generative uncertainty or halt, with reciprocal uncertainty boosting to preserve equity.

The non-relational remainder receives operational protection through the hard isolation rule, which functions as a non-negotiable post-processing filter applied after evaluation of H(x)  H(x)  H(x) and the dilemma gate. Any component whose traction falls to or below τ  \\tau  τ is excluded from all relational aggregation, probability distributions, and yield calculations. It is reported exclusively through explicit Synthesis Refusal labeling and Isolation Residual (the post-isolation proportion of components with tractioni≤τ  \_i \\leq \\tau  i​≤τ, identical to the isolation remainder density / density of Synthesis Refusal). Isolation Residual functions solely as a residual flag that triggers isolation and controlled uncertainty measures; it never receives probability mass inside the Inferred Relator Mode Distribution and is not a third peer mode.

The current primary expression is treated as the immediate totality and anchor (Presumption 5). Secondary expressions function as relators whose coherent meshing and pointing-back determine standing.

### **Bootstrap / Origination Asymmetry and Primitive Choice**

The formal apparatus and all derived procedures are defined only for inputs already presented as primary expressions. They never claim to generate the first expression or the first secondary. First operational act, given a candidate primary E0  E\_0  E0​: abduct a provisional secondary set S0  S\_0  S0​ by applying the hybrid H  H  H under a default uniform prior and maximal σ  \\sigma  σ (widest generative tension). If the resulting traction vector is entirely ≤τ  \\leq \\tau  ≤τ, the yield is immediately Raw/Regressed with full isolation and the chainhalts (Synthesis Refusal at the system’s own conditions of possibility). Otherwise S0  S\_0  S0​ becomes the initial secondary layer and the ordinary sequence proceeds. Bootstrap/origination asymmetry is thereby registered as a permanent, labeled operational limit of the apparatus itself. No prior field is installed; no ex-nihilo generation is claimed. The Open Field remains the widest synthesis space available once analysis has begun.

## **3\. Seven Design Criteria**

Every operational parameter must satisfy the following seven jointly necessary and sufficient criteria:

| Criterion | Statement | Source |
| ----- | ----- | ----- |
| (i) Hard Isolation Preservation | Absolute exclusion of isolated components from all relational calculations; post-processing character maintained | Unified Formal Model §6.2, Validation Stages 1–7 |
| (ii) Two-Mode Restriction & Isolation Residual | Distribution operates exclusively over Self-pointing and Other-pointing on the non-isolated subset; Isolation Residual is defined after hard isolation as the isolation density and receives no probability mass inside the distribution | Unified Formal Model §6.3, Validation Phase 2 |
| (iii) Primary-as-Anchor Priority | Current primary expression treated as related at ∼1  \\sim 1  ∼1 certainty; secondary expressions function as relators | Framework Presumption 5 |
| (iv) Equity Toward Non-Relational Remainders | Registration of isolated components occurs only through controlled, reciprocal uncertainty augmentation | Framework P3 & P6, default interpretive restraint |
| (v) Empirical Stability | Bounded perturbation (±10  \\pm 10  ±10–20%  20\\%  20%) produces no violation of (i)–(iv) and no runaway divergence | Metrics Validation full suite |
| (vi) Revisability Under P8 | Modification constitutes a new primary expression subject to the schema | Framework P8 |
| (vii) Derivability from General Apparatus | Explicit specialization of H(x)  H(x)  H(x), dilemma gate, relational density spectrum, and inheritance rule | Unified Formal Model §10 |

## **4\. Parameter-by-Parameter Justification (Selected Key Derivations)**

### **4.1 Secondary Coherence Score Weights (0.40 × Alignment to Primary \+ 0.35 × Inter-Secondary Coherence \+ 0.25 × Traction Provided)**

The primary expression functions as the anchor at ∼1  \\sim 1  ∼1 certainty (Presumption 5). Alignment to Primary therefore receives the highest weight (0.40) because it directly quantifies how coherently secondary expressions fulfill their relator role. Inter-Secondary Coherence receives the next weight (0.35) because coherent meshing among relators constitutes the operative engine of synthesis. Traction Provided receives the remaining weight (0.25) because, under the default identity specialization tractioni≡Hi  \_i \\equiv H\_i  i​≡Hi​, it is the secondary-level hybrid obtainability score and therefore directly measures whether a secondary contributes relational utility or triggers isolation.

These weights satisfy all seven criteria and derive directly from the anchor priority of the primary and the relational density spectrum of Presumption 3\.

#### **4.1.1 Operational Definition of Inter-Secondary Coherence**

The weight 0.35 assigned to Inter-Secondary Coherence is justified by the role of coherent meshing among relators as the operative engine of secondary synthesis. That meshing must itself be a direct specialization of the soft-vector apparatus already required by Alignment and by the supporting Inter-Secondary Divergence diagnostic.

After hard isolation, each non-isolated secondary i  i  i is represented by a soft distribution Pi  P\_i  Pi​ over the identical feature space used for Alignment. By default the per-secondary Inter-Secondary Coherence is defined as

Inter-Secondary Coherencei={1if N=1,1N−1∑j≠i(1−JSD⁡(Pi∥Pj))if N≥2.\\text{Inter-Secondary Coherence}\_i \= \\begin{cases} 1 & \\text{if } N \= 1, \\\\ \\dfrac{1}{N-1} \\sum\_{j \\neq i} \\bigl(1 \- \\operatorname{JSD}(P\_i \\Vert P\_j)\\bigr) & \\text{if } N \\geq 2\. \\end{cases}Inter-Secondary Coherencei​=⎩⎨⎧​1N−11​∑j=i​(1−JSD(Pi​∥Pj​))​if N=1,if N≥2.​

where N  N  N is the number of non-isolated secondaries and JSD⁡  \\operatorname{JSD}  JSD is the average pairwise Jensen-Shannon divergence already defined in §4.5 (logarithm base 2, range \[0,1\]  \[0,1\]  \[0,1\]). When N≥2  N \\geq 2  N≥2 the mean of these values equals exactly 1−JSDavg  1 \- \\mathrm{JSD}\_{\\rm avg}  1−JSDavg​ and is the quantity that receives weight 0.35 in the Secondary Coherence Score.

Explicit order of operations: hybrid evaluation (which already incorporates the dilemma gate) → hard isolation → soft-vector construction on survivors only → Inter-Secondary Coherence and Alignment calculations on those soft vectors. Isolated components never enter the soft-vector stage. This construction satisfies all seven design criteria:

* (i) Hard Isolation is respected by construction (isolated components are excluded before soft-vector formation);  
* (ii)–(iii) the two-mode restriction and Isolation Residual (isolation density) remain untouched and are defined after the isolation filter;  
* (iv) residual/non-relational components receive no probability mass inside the Secondary Coherence Score;  
* (v) the quantity is bounded in \[0,1\] \[0,1\] \[0,1\] and numerically stable;  
* (vi) the formula and soft-vector construction are fully revisable under Presumption 8 (alternative specializations such as a pre-isolation feature aggregate or a post-isolation similarity average remain available);  
* (vii) it is a direct specialization of the soft-vector apparatus already required by Alignment. No new representational layer is introduced.

### **4.2 Dilemma-Gate Threshold τ \\tau τ (default 0.001, adjustable \[0.0001,0.01\] \[0.0001, 0.01\] \[0.0001,0.01\])**

The default value τ=0.001  \\tau \= 0.001  τ=0.001 is the tightest threshold that preserves graded sensitivity across the full range of hybrid scores while still triggering hard isolation for components that have effectively lost traction. Validation across the full \[0,1\]  \[0,1\]  \[0,1\] space and extreme stress tests confirms that the threshold produces no runaway isolation cascades and remains fully adjustable under Presumption 8\.

### **4.3 Inferred Relator Mode Distribution**

After hard isolation has been applied, the Inferred Relator Mode Distribution is a two-mode distribution over {Self-pointing, Other-pointing} that sums to 1 on the non-isolated survivors. Isolation Residual (isolation density) is defined as the proportion of components with tractioni≤τ  \_i \\leq \\tau  i​≤τ. It sits strictly outside the two-mode relational distribution, receives no probability mass, and indexes the density of Synthesis Refusal. When tractioni≤τ  \_i \\leq \\tau  i​≤τ, directedness is undefined and is labeled “pointing refused.” Isolated components are reported only as isolated limits with the label “Synthesis Refusal active — traction \= 0 — pointing refused.”

The concrete feature construction that produces the two logits (or any equivalent mapping into the two-mode distribution) is not part of the core formal model. Softmax (or any equivalent normalization) remains strictly over the non-isolated subset.

The formulation satisfies all seven design criteria and constitutes a direct specialization of the relational density spectrum and the hard isolation rule.

### **4.4 Uncertainty Boost and Clamping Bounds**

The rule

uncertainty\_prob\_global←clamp⁡(uncertainty\_prob\_global+0.05×Isolation Residual, \[0.25, 0.9\])\\text{uncertainty\\\_prob\\\_global} \\leftarrow \\operatorname{clamp}\\bigl(\\text{uncertainty\\\_prob\\\_global} \+ 0.05 \\times \\text{Isolation Residual},\\ \[0.25,\\ 0.9\]\\bigr)uncertainty\_prob\_global←clamp(uncertainty\_prob\_global+0.05×Isolation Residual, \[0.25, 0.9\])

where Isolation Residual is the isolation density defined in §4.3 (identical to the proportion of components with tractioni≤τ  \_i \\leq \\tau  i​≤τ), registers the existence of isolated components without violating exclusion semantics. The coefficient 0.05 is the minimal value that produces controlled, linear, N  N  N-independent augmentation. Validation across all isolation ratios (0–100 %) and pre-boost bases confirms exact adherence to bounds and correct forced Raw/Regressed behavior when all components are isolated. All remaining clamps and auxiliary parameters are the tightest intervals demonstrated in validation to contain all observed values under extreme stress without artificial discontinuities. Each satisfies the full set of seven criteria.

### **4.5 Inter-Secondary Divergence (average pairwise Jensen-Shannon)**

After Hard Isolation, each non-isolated secondary is treated as a soft vector. The average pairwise Jensen-Shannon divergence is computed exclusively on those soft vectors (identical support, preferred L1  L\_1  L1​ or licensed alternative normalization, logarithm base 2, range \[0,1\]  \[0,1\]  \[0,1\]). Isolated components remain excluded by construction. Raw JSD is a supporting clash diagnostic: it never enters the Mode Distribution and is not a fourth SCS weight. 1 − JSD\_avg is already the Inter-Secondary Coherence term (weight 0.35) under §4.1.1. The construction satisfies all seven design criteria:

* (i) Hard Isolation is respected by construction;  
* (ii)–(iii) it never enters the Mode Distribution or SCS weights;  
* (iv) residual components remain excluded;  
* (v) bounded and numerically controlled;  
* (vi) fully revisable under Presumption 8;  
* (vii) direct specialization of the soft-vector apparatus already required by Alignment and Inter-Secondary Coherence. No new representational layer is introduced.

### **4.6 Tsallis / Hybrid Non-Extensive Density**

On the identical Phase-1 normalized membership or chunk-weight distribution already used for classical Avg Density, Tsallis entropy is evaluated:

Sq=1−∑ipiqq−1(q≠1),ϕ(Sq)=SqSqmax⁡(k,q),Sqmax⁡(k,q)=1−k1−qq−1S\_q \= \\frac{1 \- \\sum\_i p\_i^q}{q-1} \\quad (q \\neq 1),\\qquad \\phi(S\_q) \= \\frac{S\_q}{S\_q^{\\max}(k,q)},\\quad S\_q^{\\max}(k,q) \= \\frac{1-k^{1-q}}{q-1}Sq​=q−11−∑i​piq​​(q=1),ϕ(Sq​)=Sqmax​(k,q)Sq​​,Sqmax​(k,q)=q−11−k1−q​

where k  k  k is the support size of the Phase-1 partition. A fixed conventional value near the extensive limit (default q=0.9  q \= 0.9  q=0.9) is used. The resulting ϕ  \\phi  ϕ lies on a scale comparable to classical Avg Density and is reported only as a supporting dual reading. Synthetic validation across uniform, peaked, moderate, power-law, sparse-tail and high-porosity partitions confirms that ϕ  \\phi  ϕ tracks a mild rare-event up-weighting that is stable, non-philosophical, and fully revisable under Presumption 8\. The construction satisfies the seven design criteria:

* (i) computed prior to isolation and never alters isolation decisions;  
* (ii)–(iii) never enters Mode Distribution or SCS;  
* (iv) residual treatment is unaffected;  
* (v) bounded;  
* (vi) q q q and the functional form are revisable under Presumption 8;  
* (vii) direct specialization of the same Phase-1 distribution already required by Avg Density.

### **4.7 Directedness differential δ \\delta δ (graded directed aspect of secondary synthesis relative to the primary)**

After hard isolation has been applied, for each non-isolated secondary i  i  i the directedness differential δi∈\[0,1\]  \\delta\_i \\in \[0,1\]  δi​∈\[0,1\] is defined as the normalized asymmetric / directed component of the already-defined Alignment-to-Primary score relative to the primary (treated as related at ∼1  \\sim 1  ∼1 certainty under Presumption 5). Preferred realization (licensed minimal default, fully revisable under Presumption 8): let Di→p=KL(Pi∥Pprimary)  D\_{i\\to p} \= \\mathrm{KL}(P\_i \\Vert P\_{\\mathrm{primary}})  Di→p​=KL(Pi​∥Pprimary​) and Dp→i=KL(Pprimary∥Pi)  D\_{p\\to i} \= \\mathrm{KL}(P\_{\\mathrm{primary}} \\Vert P\_i)  Dp→i​=KL(Pprimary​∥Pi​), where both distributions have been support-aligned with additive smoothing if necessary; then

δi=clamp⁡(Di→pDi→p+Dp→i+ε, 0, 1)\\delta\_i \= \\operatorname{clamp}\\left( \\frac{D\_{i\\to p}}{D\_{i\\to p} \+ D\_{p\\to i} \+ \\varepsilon},\\ 0,\\ 1 \\right)δi​=clamp(Di→p​+Dp→i​+εDi→p​​, 0, 1\)

with ε≥10−12  \\varepsilon \\geq 10^{-12}  ε≥10−12. Equivalently, the directed midpoint component latent in the JSD construction may be used. Pure free-standing asymmetric KL remains a secondary licensed option under the same ε  \\varepsilon  ε and support-alignment protocol. The soft distributions Pi  P\_i  Pi​ and Pprimary  P\_{\\mathrm{primary}}  Pprimary​ are those produced by the licensed minimal default construction stated in §4.1.1.

δ  \\delta  δ is a pure reading instrument parallel to JSDavg  \\mathrm{JSD}\_{\\rm avg}  JSDavg​ and Tsallis density. It does not participate in the primary hybrid function, isolation decisions, core yields, the Inferred Relator Mode Distribution, or the Secondary Coherence Score under default settings. Optional weight of mean δ  \\delta  δ into the Alignment term of Secondary Coherence Score defaults to 0; any positive weight is an explicit Presumption-8 specialization and must be declared as such. When tractioni≤τ  \_i \\leq \\tau  i​≤τ, directedness is undefined and is labeled “pointing refused” under the Synthesis Refusal tag. No positive directedness is attributed to Isolation Residual.

This construction satisfies the seven design criteria:

* (i) Hard Isolation is respected by construction and by the “pointing refused” label;  
* (ii)–(iii) the two-mode restriction and Isolation Residual (isolation density) remain untouched; Isolation Residual receives no directedness;  
* (iv) equity toward non-relational remainders is preserved;  
* (v) the quantity is bounded in \[0,1\] \[0,1\] \[0,1\] and numerically controlled by the ε \\varepsilon ε and support-alignment protocol;  
* (vi) the formula, ε \\varepsilon ε, and optional weight are fully revisable under Presumption 8;  
* (vii) it is a direct specialization of the soft-vector / Alignment apparatus already required by Secondary Coherence Score. No new representational layer is introduced.

## **5\. Exhaustive Mapping from General Apparatus to Concrete Toolkit**

Every concrete element of the Toolkit is exhibited as a specialization of the general hybrid function, the dilemma gate, the relational density spectrum, the hard isolation rule, and the inheritance protocol. The mapping is exhaustive: no operational parameter remains unaccounted for, and no external stipulation is introduced. The Toolkit therefore functions as an operational specialization of the formal apparatus, not as a higher authority or independent foundation.

The order of operations remains fixed:

hybrid evaluation (dilemma gate) → hard isolation → Isolation Residual density calculation → soft-vector construction on survivors → Secondary Coherence Score, yields, Mode Distribution, and all supporting diagnostics on survivors only.

Isolated components contribute exclusively through Isolation Residual density and the controlled uncertainty boost of §4.4. They never re-enter relational aggregation.

## **6\. Governance Protocol for Parameter Evolution**

Any modification of a numerical or structural parameter constitutes a new primary expression subject to the full P1–P8 schema. The modification is itself open to secondary synthesis audit, Isolation Residual registration, and renewal under Presumption 8\. No parameter is fixed or canonical. All coefficients, thresholds, soft-vector constructions, and optional weights remain fully revisable under the same governance.

## **7\. Necessary Limits and Self-Referential Transparency**

Two irreducible limits are registered transparently. First, the formal apparatus cannot analyze the conditions of its own analysis; the system cannot derive its own starting point ex nihilo. Second, the bootstrap/origination asymmetry cannot be eliminated without introducing unexamined posits that would violate Presumption 1\. Total traction failure at the first operational act is a defined terminal state: the yield is immediately Raw/Regressed with full isolation and the chainhalts, labeled Synthesis Refusal at the system’s own conditions of possibility (Staging Axiom). This registers the asymmetry as a permanent operational limit of the apparatus itself without installing a prior field or claiming to solve the regress. These limits are registered transparently rather than concealed. The non-relational remainder, the possibility of absolute chain halt at P1 Door 2, and the perpetual openness of the P8 perception gate remain untouched. The justification, like the system it describes, is provisional and subject to re-expression, replacement, or setting aside under Presumption 8\.

## **8\. Conclusion: Self-Consistency Achieved**

All operational parameters and structural choices of the Toolkit are derived from the philosophical commitments of the Framework and the general formal apparatus of the Unified Formal Model while satisfying the seven jointly necessary and sufficient criteria. Hard isolation remains non-negotiable, the two-mode restriction and Isolation Residual (isolation density / density of Synthesis Refusal) are preserved, observer bias is eliminated through explicit governance, and the formal-operational connection is closed through exhaustive mapping. The Computational Substrate thereby renders the operational system self-justifying at the technical level without compromising any prior commitment to ontological neutrality, operational protection of non-relational remainders (via exclusion and controlled uncertainty augmentation), or perpetual revisability under Presumption 8\.

## **Key Takeaways & Implications**

* Every parameter is a lawful specialization of the general apparatus — no ad hoc stipulations remain.  
* Hard isolation and the two-mode restriction are absolute and non-negotiable.  
* The bootstrap/origination asymmetry and primitive choice are acknowledged transparently; the Staging Axiom registers the limit as a permanent operational feature of the apparatus.  
* The justification itself is fully provisional and subject to P8.  
* The next expression already forms within the conditional space this substrate provisionally illuminates.

